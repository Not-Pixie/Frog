export type Movimentacoes = {
    mov_id: number,
    codigo: number,
    tipo: string,
    link: string,
    carrinho_id: number,
    valor_total: number,
    comercio_id: number,
    criado_em: string
}